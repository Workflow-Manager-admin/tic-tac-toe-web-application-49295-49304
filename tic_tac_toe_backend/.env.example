# Example environment variables for Tic Tac Toe backend

# Database configuration (SQLite by default)
DB_URL=sqlite:///./tic_tac_toe.db

# JWT key for authentication (change in production!)
JWT_SECRET_KEY=your-secret-key
